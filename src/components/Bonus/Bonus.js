function Bonus({ title, bodyText }) {
  return `
      <div class="Bonus">
        <h1 class="Bonus-title">${title}</h1>
        <p class="Bonus-body">${bodyText}</p>
      </div>
    `;
}

export default Bonus;
